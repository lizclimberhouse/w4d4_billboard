<h1>Click to View Artist: "<%= @artist.name %>" Songs</h1>
<%= link_to 'Create a new song', new_artist_song_path(@artist) %>
<ul>
  <% @songs.each do |song| %>
    <li>
    <%= link_to song.name, [@artist, song] %>
    </li>
  <% end %>
</ul>
<%= link_to 'Back to artist', artist_path(@artist) %>